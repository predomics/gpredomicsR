% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visu.R
\name{plotPrevalence}
\alias{plotPrevalence}
\title{Plot Feature Prevalence and Enrichment}
\usage{
plotPrevalence(
  features,
  X,
  y,
  topdown = TRUE,
  main = "",
  plot = TRUE,
  col.pt = c("deepskyblue4", "firebrick4"),
  col.bg = c("deepskyblue1", "firebrick1"),
  zero.value = 0
)
}
\arguments{
\item{features}{A character vector of feature names to be plotted.}

\item{X}{A data matrix or data frame where \strong{rows = features} and \strong{columns = samples}.}

\item{y}{A vector of class labels (e.g., \code{1} and \code{-1} for binary classification)
corresponding to the columns in \code{X}.}

\item{topdown}{Logical; if \code{TRUE}, features are displayed in descending order (default: \code{TRUE}).}

\item{main}{A string for the title of the plot.}

\item{plot}{Logical; if \code{TRUE}, the function \strong{displays the plot}. If \code{FALSE},
it \strong{returns the enrichment statistics} instead of plotting.}

\item{col.pt}{Colors for points in the plot (default: \code{c("deepskyblue4", "firebrick4")}).}

\item{col.bg}{Colors for bars in the plot (default: \code{c("deepskyblue1", "firebrick1")}).}

\item{zero.value}{The value treated as zero in prevalence calculations (default: \code{0}).}
}
\value{
If \code{plot = TRUE}, returns a \code{ggplot2} object displaying feature prevalence.
If \code{plot = FALSE}, returns the \strong{enrichment results} (statistical test output).
}
\description{
This function visualizes the prevalence of features across different groups
in a dataset and computes feature enrichment, providing optional statistical
tests for enrichment. If enrichment data is available, significance markers are added.
}
\examples{
features <- c("feature1", "feature2", "feature3")
X <- matrix(sample(0:1, 300, replace = TRUE), nrow = 3)
rownames(X) <- features
y <- sample(c(1, -1), 100, replace = TRUE)

# Plot feature prevalence
plotPrevalence(features, X, y, main = "Feature Prevalence Plot")

# Get enrichment statistics without plotting
plotPrevalence(features, X, y, plot = FALSE)

}
\author{
Edi Prifti (IRD)
}
