% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{computeCardEnrichment}
\alias{computeCardEnrichment}
\title{Compute Feature Enrichment Using Chi-Square Test}
\usage{
computeCardEnrichment(v.card.mat, y)
}
\arguments{
\item{v.card.mat}{A \strong{matrix or data frame}, where \strong{columns = features} and
\strong{rows = classes}, indicating the count of non-zero occurrences in each class.}

\item{y}{A \strong{vector of class labels} (binary classification) corresponding to the samples.}
}
\value{
A \strong{list} containing:
\itemize{
\item \verb{$card.all} : Class sample sizes
\item \verb{$chisq.p}  : \strong{Chi-Square p-values} for feature-class associations
\item \verb{$chisq.q}  : \strong{Adjusted p-values} (Bonferroni correction)
\item \verb{$v.card.mat} : The original input matrix or data frame (converted if necessary)
\item \verb{$y} : The class labels
}
}
\description{
This function computes \strong{feature enrichment statistics} for a \strong{two-class} vector
using \strong{Chi-Square tests}. It evaluates whether the \strong{cardinality} of a feature
(presence/absence) is significantly associated with class labels.
}
\examples{
v.card.mat <- matrix(c(30, 50, 20, 60), nrow = 2, byrow = TRUE)
rownames(v.card.mat) <- c("class1", "class2")
colnames(v.card.mat) <- c("feature1", "feature2")
y <- c(rep(1, 50), rep(0, 50))  # Binary class labels

computeCardEnrichment(v.card.mat, y)

}
