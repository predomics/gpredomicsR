% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment.R
\name{runExperiment}
\alias{runExperiment}
\title{Run a Predomics Experiment}
\usage{
runExperiment(
  param.path = "sample/param.yaml",
  name = "",
  glog_level = "debug"
)
}
\arguments{
\item{param.path}{A string specifying the path to the parameter file (YAML format).
Default is \code{"sample/param.yaml"}.}

\item{name}{an optional string to name the experiment.}

\item{glog_level}{the verbose level for glog (default: "debug").}
}
\value{
A list containing:
\item{rust}{A list with Rust objects: the experiment, parameters, and running flag.}
\item{params}{The parameter settings loaded from the YAML file.}
\item{data}{A list with training and testing datasets.}
\item{model_collection}{An R representation of the experiment's model evolution.}
\item{execTime}{The execution time of the experiment in minutes.}
}
\description{
This function executes an experiment using the \code{gpredomicsR} package by loading
experiment parameters, setting up a logging mechanism, and running a genetic
algorithm. It returns a structured list containing the experiment results.
}
\details{
The function follows these steps:
\itemize{
\item Saves the current working directory and switches to the experiment directory.
\item Initializes the Rust logger (idempotent; repeated calls are safe).
\item Loads parameters from the YAML file.
\item Runs the genetic algorithm to generate and evaluate models.
\item Constructs and returns an experiment object containing key components.
}
}
\examples{
\dontrun{
exp <- runExperiment(param.path = "sample/param.yaml")
print(exp)
}

}
\author{
Edi Prifti (IRD)
}
