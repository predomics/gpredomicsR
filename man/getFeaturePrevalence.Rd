% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{getFeaturePrevalence}
\alias{getFeaturePrevalence}
\title{Compute Feature Prevalence in a Dataset}
\usage{
getFeaturePrevalence(features, X, y = NULL, prop = TRUE, zero.value = 0)
}
\arguments{
\item{features}{A character vector (feature names) or numeric vector (feature indices).}

\item{X}{A data matrix or data frame where \strong{rows = features} and \strong{columns = samples}.}

\item{y}{An optional vector of class labels. If provided, prevalence is computed \strong{per class}.}

\item{prop}{Logical; if \code{TRUE}, prevalence is computed as \strong{proportion} instead of raw counts (default: \code{TRUE}).}

\item{zero.value}{The value considered as \strong{zero} in the dataset (useful for log-transformed data, default: \code{0}).}
}
\value{
A \strong{list} with prevalence values:
\itemize{
\item \code{"all"}: Prevalence in the entire dataset.
\item \code{"<class_label>"}: Prevalence per class if \code{y} is provided.
}
}
\description{
This function computes the \strong{prevalence of selected features} across the entire dataset
and, if provided, across different classes.
}
\examples{
features <- c("gene1", "gene2", "gene3")
X <- matrix(sample(0:1, 300, replace = TRUE), nrow = 3)
rownames(X) <- features
y <- sample(c("A", "B"), 100, replace = TRUE)

# Compute prevalence in full dataset
getFeaturePrevalence(features, X)

# Compute prevalence per class
getFeaturePrevalence(features, X, y)

}
\author{
Edi Prifti (IRD)
}
